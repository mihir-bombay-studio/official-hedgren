{{ 'hedgren-hero.css' | asset_url | stylesheet_tag }}
{{ 'hedgren-collection-hero.css' | asset_url | stylesheet_tag }}
{{ 'hedgren-discover-collection.css' | asset_url | stylesheet_tag }}
{{ 'hedgren-coll-list-hero.css' | asset_url | stylesheet_tag }}

<div class="hero hedgren-coll-list-hero" style="{% if section.settings.image %}background-image: url({{ section.settings.image | img_url: "2000x" }}){% else %}background: #375D54{% endif %}">
  <div class="container">
    	<h1 class="coll-list-heading">
        {{ section.settings.heading_main }}
      </h1>
    <div class="coll-list-sub-heading">{{ section.settings.sub_heading }}</div>
  </div>
</div>
<div class="hero-products-wrapper hedgren-collections-wrapper">
  <div class="container rel">
     <span class="image-overlay">
        {% include "svg-arrows-big" %}
      </span> 
    <div class="collection-list-wrap flex-box justify-between">
      {% if settings.h_collection1 %}
      {% assign coll = settings.h_collection1 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection1 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                <a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
                <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection2 %}
      {% assign coll = settings.h_collection2 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection2 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
                <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection3 %}
      {% assign coll = settings.h_collection3 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection3 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
                <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection4 %}
      {% assign coll = settings.h_collection4 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection4 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
              <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection5 %}
      {% assign coll = settings.h_collection5 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection5 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
               <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection6 %}
      {% assign coll = settings.h_collection6 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection6 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
              <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection7 %}
      {% assign coll = settings.h_collection7 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection7 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
               <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection8 %}
      {% assign coll = settings.h_collection8 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection8 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
              <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection9 %}
      {% assign coll = settings.h_collection9 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection9 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
              <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection10 %}
      {% assign coll = settings.h_collection10 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection10 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
              <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection11 %}
      {% assign coll = settings.h_collection11 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection11 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
               <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection12 %}
      {% assign coll = settings.h_collection12 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection12 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
              <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection13 %}
      {% assign coll = settings.h_collection13 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection13 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
              <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection14 %}
      {% assign coll = settings.h_collection14 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection14 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
               <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection15 %}
      {% assign coll = settings.h_collection15 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection15 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
               <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
      {% if settings.h_collection16 %}
      {% assign coll = settings.h_collection16 %}{% unless coll.handle == collection.handle %}
    <div class="featured-coll-card">
      {% assign collection_handle = settings.h_collection16 %}
      		<div class="coll-card">
              <div class="coll-img">
              	{% if collection_handle.image %}
                	<a href="{{ collection_handle.url }}"><img loading="lazy" src="{{ collection_handle.image | img_url: '350x' }}" alt="{{ collection_handle.title }}"></a>
                {% else %}
                	{{ 'collection-1' | placeholder_svg_tag }}
				{% endif %}
              </div>
              <div class="coll-text">
                <div class="coll-title">{{ collection_handle.title }}</div>
              <div class="coll-desc">{% if collection_handle.description != "" %}{{ collection_handle.description }}{% else %}Your favorite everyday companion. Ready-to-go bags and travel gear for everyday use.{% endif %}</div>
               <span class="free-radical"><a class="coll-url" href="{{ collection_handle.url }}"><span class="coll-url-text">DISCOVER COLLECTION</span><span class="right-chev-svgs">{% include 'icon-red-right-chevron' %}{% include 'icon-red-right-chevron' %}</span></a></span>
              </div>
      </div>
    </div>{% endunless %}
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
  {
    "name": "Hedgren Coll List Hero",
    "settings": [
      {
         "type":"image_picker",
         "id":"image",
         "label":"Image"
      },
		{
			"type": "text",
			"id": "heading_main",
			"label": "Heading Main"
		},
		{
			"type": "text",
			"id": "sub_heading",
			"label": "Sub Heading"
		}
     ],
	"blocks": [
		{
			"type": "collect",
			"name": "Collection",
			"settings": [
				{
                    "id": "coll",
                    "type": "collection",
                    "label": "Select Collection"
                },
				{
                    "id": "page",
                    "type": "url",
                    "label": "Select Collection Page"
                }
			]
		}
	],
   "presets":[
      {
         "name":"Hedgren Coll List Hero"
      }
   ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
